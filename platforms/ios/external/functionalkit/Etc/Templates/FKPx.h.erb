#import <Foundation/Foundation.h>
<% 
init_bit = (1..n).to_a.map { |i| "_#{i}:(id)new#{selectors[i-1]}" }.join(' ') 
%>

@interface FKP<%= n %> : NSObject

<%= (1..n).to_a.map { |i| "@property (readonly) id _#{i};" }.join("\n") %>

- (id)initWith<%= init_bit %>;
+ (id)<%= init_bit %>;
@end
